// import { selectFilterResultsDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'chai';

// const selector = selectFilterResultsDomain();

describe('selectFilterResultsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
